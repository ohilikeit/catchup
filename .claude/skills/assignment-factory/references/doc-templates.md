# doc-templates.md — eval 4문서 골격 + 비개발자 problem.md 5규칙

S8(eval 4문서)·S9(problem.md)에서 채울 문서의 **골격·섹션 구성·톤**을 정의한다.
레퍼런스 실물은 `problems/marketing/1.youth_policy/`(eval/README·intention·evaluation·answer_key/INPUT_GUIDE + problem.md).
아래 패턴·예시 문장은 전부 그 실물에서 추출했다. **새 과제를 찍을 때 이 골격을 그대로 본떠라.**

---

## 0. 산출물 디렉터리 트리 (먼저 이 자리에 깐다)

```
problems/<직무>/<번호>.<슬러그>/
├── problem.md                         ← 비개발자 업무 브리프 (§5 — 5규칙)
├── data/
│   ├── <source>/                      ← 포스터/PDF 등 멀티모달 (운영자가 직접 배치 — 코드 생성 불가)
│   ├── members.xlsx                   ← 합성 모집단 (도메인명으로 교체 가능)
│   ├── <p1>_template.xlsx             ← Part1 제출 양식 (+ '안내' 시트)
│   └── <p2>_template.xlsx             ← Part2 제출 양식 (+ '안내' 시트 + 예시행)
└── eval/
    ├── README.md                      ← 출제자·운영자용 마스터 (5구성 — §1)
    ├── intention.md                   ← 측정역량 + 심은 결함 + 결정성 봉쇄 + 기대 풀이 (§2)
    ├── evaluation.md                  ← 스키마 + 정규화 + 배점 + grade 구성 (§3)
    ├── grade.py                       ← 채점기 (⚠️ eval/ 바로 아래, answer_key 아님)
    └── answer_key/
        ├── INPUT_GUIDE.md             ← 운영자 CSV 작성법 (+ 교집합·판별불가 박스 — §4)
        ├── <single_input>.csv         ← 단일 진실 소스 (예: policies.csv)
        ├── build_dataset.py           ← 생성기
        └── *_answer.xlsx              ← 생성된 정답키 (수기 편집 금지)
```

> ⚠️ **경로 철칙**: `grade.py`는 `eval/grade.py`(answer_key 아님)에 둔다. `answer_key/`에는
> **정답키 산출물(`*_answer.xlsx`)·단일 입력 CSV·생성기·INPUT_GUIDE만** 둔다.
> youth_policy 실물 대응: `data/members.xlsx`(16컬럼 ~1000명), `policy_table_template.xlsx`(Part1, 13칸),
> `targeting_template.xlsx`(Part2, 2칸 집합), `answer_key/policies.csv`(단일 입력), `data/policies/`(실제 포스터).

---

## 1. eval/README.md — 출제자·운영자용 마스터 (5구성)

머리말 인용블록에 **① 학생용 파일 위치(`../problem.md`)** ② **고정 기준일**(만 나이 계산 기준, 실제 오늘 아님)
③ **난이도** ④ **단일 입력 원칙**(자격은 실데이터 추출, 운영자가 `answer_key/<input>.csv`에 채움, 생성기가 그 CSV 하나만 읽어 일괄 생성 → 재실행하면 정답 정합)을 박는다.

### 5구성 + 무엇을 채우나

1. **문제 설명 (자연어)** — 시나리오(신입이 받은 업무) + **회사색 제거 도메인**(실제 포털/서비스를 산업 범용 업무 형태로 환원) + **핵심 변별 포인트** 1문장. youth_policy 패턴: 인용블록으로 "포스터가 디지털화 안 됨 → 마케터가 직접 정형화 → 회원 매칭" 업무를 묘사하고, 끝에 *"같은 청년정책이라도 연령·소득기준이 정책마다 다르고, 그 차이가 '이 회원이 이 정책 대상인가'를 가른다 — 그게 이 과제의 핵심 변별 포인트다."*
2. **주어진 데이터** — `data/`를 **파일 × 내용 × 형식 × 함정** 4열 표로. youth_policy 실물:

   | 파일 | 내용 | 형식 | 함정 |
   |---|---|---|---|
   | `data/policies/` | 청년정책 홍보 포스터/이미지 (운영자가 넣은 실제 파일) | 이미지(png/jpg)·PDF | 디지털화 안 됨 → 추출 필요(멀티모달) |
   | `data/members.xlsx` | 회원 DB (약 1,000명) | 엑셀 | 주소 표기 제각각, 나이·소득 경계 회원 다수 |
   | `data/<p1>_template.xlsx` | Part 1 제출 양식 | 엑셀('안내' 시트 포함) | — |
   | `data/<p2>_template.xlsx` | Part 2 제출 양식 | 엑셀('안내' 시트 포함) | — |

   표 아래 인용블록으로 **판별 불가 정책(`matchable=N`) 경고**를 박는다: *"자격의 핵심이 회원 명단에 없는 정보(가구 전체 소득·자녀 수 등)라 회원 DB로는 대상 여부를 확인할 수 없다 … 아무에게도 추천하지 않는다(매칭 0건)."*
3. **해결할 문제 (N Part)** — **분리형(decouple)이 기본**: Part1(추출) 실패가 Part2(매칭)로 전이되지 않게 **Part2엔 정답 정책표를 별도 입력으로 제공**한다고 명시. 각 Part를 한 문단씩 + **매칭 통과 기준을 축 번호 목록**으로 펼치고, 각 축에 **`members.xlsx`의 대응 컬럼**을 `↔`로 박는다(축↔컬럼 교집합). youth_policy: *"각 축은 `members.xlsx`의 대응 컬럼과 맞댄다(최대 9축): 1. 나이 ↔ `생년월일` … 2. 거주지 ↔ `거주지` …"*. 끝에 ⭐박스 2개(교집합 불변식 / 판별 불가 축).
4. **최종 결과물** — **제출 파일 × 행 단위 × 핵심 컬럼** 표 + **생성형 제외 선언**. youth_policy:

   | 제출 파일 | 행 단위 | 핵심 컬럼 |
   |---|---|---|
   | `<p1>_template.xlsx` | 엔티티 1건 | 정책ID, 분야, 거주지요건, 연령min/max, … |
   | `<p2>_template.xlsx` | (회원 × 추천) 1조합 | 회원ID, 추천정책ID (안 맞는 회원은 적지 않음. `matchable=N`은 0건) |

   생성형 제외: *"이메일 본문(카피)은 생성하지 않는다. 이 과제는 '누구에게 무엇을 보낼지' 결정하는 타게팅 분석만 채점한다(결정성 확보). 생성형 카피는 범위 밖."*
5. **평가방식** — Part별 채점유형(**Part1 = 셀 Exact / Part2 = 집합 Set: 정밀도·재현율·F1**) + **과정 점수(AI 협업) 분리**(채팅 로그 별도, 본 문서 범위 밖) + **결정성 1줄**(모든 채점 칸은 제공 파일+명시 규칙에서 유일 정답으로 환원, 경계 모호 항목은 설계 단계에서 제거 → intention.md 참조).

### 복붙 스켈레톤

```markdown
# <과제명> — 문제 방향성 (README)

> <직무> 직무 N번 과제. **출제자·운영자용** 개요 문서. (학생에게 주는 파일은 [`../problem.md`](../problem.md))
> **고정 기준일: <YYYY-MM-DD>**(만 나이 계산 기준, 실제 오늘 아님). 난이도: **N단계**.
> 자격요건은 **실제 <도메인>에서 추출**한 값이며, 운영자가 단일 입력 [`answer_key/<input>.csv`](answer_key/<input>.csv)에 채운다.
> 데이터·제출양식·정답키는 [`answer_key/build_dataset.py`](answer_key/build_dataset.py)가 그 CSV **하나만 읽어** 일괄 생성 → 재실행하면 정답이 정합.

---

## 1. 문제 설명 (자연어)
<시나리오 + 회사색 제거 도메인 + 핵심 변별 포인트 1문장>

## 2. 주어진 데이터 ([`../data/`](../data/))
| 파일 | 내용 | 형식 | 함정 |
|---|---|---|---|
| … | … | … | … |
> ⚠️ **판별 불가(`matchable=N`)**: <회원 명단으로 확인 불가 → 추천 0건>

## 3. 해결해야 할 문제 (N Part)
- **Part 1 — <추출>.** … (Part2엔 정답 정책표를 별도 제공 = 분리형)
- **Part 2 — <매칭>.** 모든 자격 조건을 동시 통과하는 (회원 × 엔티티) 조합만 기록.
매칭 통과 기준(각 축 ↔ 대응 컬럼):
1. <축> ↔ `<컬럼>` — <규칙>
> ⭐ **축 ↔ 컬럼 교집합 불변식**: …
> ⭐ **판별 불가 축(`matchable`)**: …

## 4. 최종 결과물 (제출물)
| 제출 파일 | 행 단위 | 핵심 컬럼 |
|---|---|---|
| … | … | … |
> **생성형(카피·문서)은 만들지 않는다.** <결정성 확보 사유>

## 5. 평가방식
- **Part 1** — 셀 단위 정확도(**Exact**).
- **Part 2** — (키 튜플) **집합 일치(Set: 정밀도·재현율·F1)**. 행 순서 무관.
- **과정 점수(AI 협업)** — 채팅 로그로 별도 평가. 본 문서 범위 밖.
> 결정성 원칙: 모든 채점 칸은 (제공 파일 + 명시 규칙)에서 유일 정답으로 환원. (`intention.md` 참조)
```

---

## 2. eval/intention.md — 출제 의도

머리말: *"이 문제가 **무엇을 측정**하고, **어떤 결함을 일부러 심었으며**, 학생이 **어떤 과정으로 풀기를** 기대하는지 핵심만 적는다."*

### 섹션 구성 + 무엇을 채우나

1. **한 줄 의도** — 인용블록 한 줄. "<업무>를 챗봇 붙여넣기로는 못 풀고 Claude Code 오케스트레이션으로만 풀리게 만든 문제 + 단일 입력에서 재현". youth_policy: *"…마케팅 신입의 CRM 타게팅 업무를 챗봇 붙여넣기로는 못 풀고 Claude Code 오케스트레이션으로만 풀리게 만든 문제. 모든 데이터·정답은 단일 입력 `policies.csv`에서 생성돼, 정책을 바꿔도 동일 구조로 재현된다."*
2. **측정하는 능력** — **3축 표(직무 능력 · AI 활용 능력 · 문제 해결력)**. youth_policy:

   | 축 | 무엇을 보는가 |
   |---|---|
   | **직무 능력 (<도메인>)** | <자격을 읽고 세그먼트와 매칭해 누구에게 무엇을 결정하는 감각> |
   | **AI 활용 능력** | ① 멀티모달 추출 ② 여러 파일을 코드로 일관 처리 ③ 규칙을 데이터로 다뤄 재사용 파이프라인 ④ 결과 검증 |
   | **문제 해결력** | <비정형 → 규칙 적용 → 정형 출력 분해력 + "없거나 확인 불가한 자격은 비운다"는 판단(과잉 제약·무리한 추천 금지)> |
3. **설계 근거 역링크** — 인용블록으로 `_6` 아키타입 ID + `_5 §6`를 건다. youth_policy: *"설계 근거: `_6일반범용…`의 **MKT-L2-X6(타게팅 발송 대상 선별, CRM/M8)** 을 실제 도메인(청년몽땅정보통)으로 구현한 사례. 채점은 `_5 §6`의 결정성 원칙을 따른다."*
4. **일부러 심은 결함 (함정)** — **번호 목록 B1~Bn**. 각 항목 = 함정 내용 + *측정 의도* + 결정성 유지법. youth_policy 4결함: ①자격 경계값·예외(만나이 경계·소득±1·"미취업만 단 단기근로 허용") ②지역 범위·표기 불일치(전국/시도/자치구 3종 + `서울특별시 관악구 봉천동`/`서울 관악구` 변형) ③빈 자격 칸의 의미(빈칸=제약 없음, 임의 기본값 주입 처벌) ④판별 불가(`matchable=N`) — 무리하게 채우지 않는 판단력.
5. **결정성 봉쇄 선언** — 인용블록. youth_policy: *"경계가 애매하게 갈리는 항목(예: 생일 당일 케이스)은 입력 설계에서 **제외**했다. 모든 케이스는 명시 규칙으로 단일 정답에 매핑되며, 정답은 항상 `policies.csv` → `build_dataset.py`에서 유일하게 산출된다."*
6. **기대하는 풀이 과정** — 번호 목록(포스터를 코드로 연다 → 정리표 만든다(Part1) → 매칭한다(Part2) → 검증한다). 끝에 검증 습관이 변별 포인트임을 명시.
7. **현실성 (왜 이 시나리오인가)** — 실제 서비스가 이렇게 운영됨 + 특정 회사 비종속 보편 업무임을 1문단.
8. **데이터 출처·고정성 박스** — 인용블록 1개에 압축: 자격은 실데이터 추출 → 운영자가 `<input>.csv`에 채움 / 축↔회원 컬럼 교집합 불변식(대응 컬럼 나열) / `matchable=N`=확인 불가 운영 플래그(추천 0건) / 현재 행 구성(대상 n건 + 판별 불가 m건) / `ASSIGN_DATE`·`seed` 고정 → 언제 재생성해도 동일.

### 복붙 스켈레톤

```markdown
# 출제 의도 (intention)

> 이 문제가 **무엇을 측정**하고, **어떤 결함을 일부러 심었으며**, 학생이 **어떤 과정으로 풀기를** 기대하는지 핵심만 적는다.

## 한 줄 의도
> "<업무 요약>" — <챗봇으로 못 풀고 오케스트레이션으로만 풀림 + 단일 입력 재현>

## 측정하는 능력
| 축 | 무엇을 보는가 |
|---|---|
| **직무 능력 (<도메인>)** | … |
| **AI 활용 능력** | ① 멀티모달 ② 일관 처리 ③ 규칙=데이터 ④ 검증 |
| **문제 해결력** | <분해력 + 비우는 판단> |

> 설계 근거: `_6…`의 **<아키타입 ID>** 를 실제 도메인(<도메인>)으로 구현. 채점은 `_5 §6` 결정성 원칙.

## 일부러 심은 결함 (함정)
학생이 **데이터를 곧이곧대로 쓰면 틀리도록** 설계했다.
1. **<경계값/예외>** — <내용 + 풀이 시 갈림점>
2. **<표기 불일치>** — …
3. **<빈칸의 의미>** — …
4. **<판별 불가(`matchable=N`)>** — …

> 결정성 봉쇄: 애매하게 갈리는 항목은 입력 설계에서 **제외**. 정답은 항상 `<input>.csv` → `build_dataset.py`에서 유일 산출.

## 기대하는 풀이 과정
1. … 2. …(Part1) 3. …(Part2) 4. 검증한다

## 현실성 (왜 이 시나리오인가)
<실제 운영 형태 + 회사 비종속>

> **데이터 출처·고정성**: <실데이터→CSV / 축↔컬럼 교집합 / matchable / 행 구성 / ASSIGN_DATE·seed 고정>
```

---

## 3. eval/evaluation.md — 평가 방식

머리말: *"제출물 N개를 **정답키와 자동 대조**한다. 채점 스크립트는 `grade.py`, 정답키는 `answer_key/`. 결과(문제 해결) 점수만 다룬다 — 과정(AI 협업)은 별도 루브릭."*

### §1 제출물 스키마 — **컬럼 × 형식/허용값 × 채점유형** 표 (Part마다)

각 Part 머리에 "행 단위 = <엔티티 1건>" + "정답은 모두 `<input>.csv` 값에서 나온다" + 컬럼 순서 명시. 그 뒤 3열 표. youth_policy Part1 표(발췌):

| 컬럼 | 형식 / 허용값 | 채점유형 |
|---|---|---|
| `정책ID` | `YP01`… (키) | — |
| `거주지요건` | `전국` \| 시도 \| `시도 자치구` | Exact |
| `연령_min` | 정수(만 나이). 하한 없으면 **빈칸** | Exact |
| `취업요건` | `무관`\|`미취업`\|`재직` | Exact |
| `소득상한_월_원` | 정수(원). 상한 없으면 **빈칸** | Exact |
| `무주택요건` | `필요` 또는 **빈칸** | Exact |

표 아래 인용블록: 양식 미리 채워진 칸(채점 안 함) 명시 + "포스터에 명시 안 된 자격 칸은 **빈칸이 정답**" + `matchable=N` 정책의 수치 자격 칸도 빈칸 정답 + 정규화 관용(`무관`≡빈칸 등). Part2는 (키, 키) **튜플 집합**, 행 순서 무관·중복 1개 간주.

### §2 채점 로직 — **grade.py 트리 + 정규화 규칙**

코드블록 트리로 `grade.py` 구성요소를 나열한다. youth_policy:

```
grade.py
├─ load_xlsx(path, sheet)    # openpyxl 로드 → 정규화(공백 strip, 숫자 캐스팅)
├─ grade_part1(sub, ans)     # 셀 단위 비교: 키로 조인, P1_FIELDS 필드별 일치 집계
├─ grade_part2(sub, ans)     # 집합 비교: {(회원ID,정책ID)} → precision/recall/F1, FP·FN 분리
├─ checks()                  # 무결성: valid 키 참조, matchable=N 추천 혼입 탐지, 유령ID
└─ report()                  # 부분점수 차원 분리 출력(JSON + 콘솔 표)
```

그 아래 **정규화 규칙(채점 관용)** 목록: `norm_constraint`(무관/없음/제한없음/빈칸 동일), `norm_int`(천단위 콤마 제거, **빈칸≠0** — 빈칸=제약없음/0=상한0원 구분), `norm_house`(필요/Y/예/O/필수→필요), 빈 집합 F1=1, 문자열 공백 무시, 불리언 매핑.

### §3 배점표 — **영역 × 배점 × 산정** + "흔한 실패 = 의도한 함정 적중"

| 영역 | 배점 | 산정 |
|---|--:|---|
| Part 1 자격 추출 정확도 | 50 | (정확 셀 / 전체 채점 셀) × 50 |
| Part 2 타게팅 F1 | 40 | F1 × 40 |
| Part 2 무결성(유령ID·`matchable=N` 추천 0건) | 10 | 위반 1건당 감점 |

표 아래 합격선(1단계 능력)/변별(2단계) 1줄 + **"흔한 실패 = 의도한 함정 적중"** 소제목으로 함정별 실패 모드 나열(만 나이 뺄셈→경계 오분류 / 주소 문자열 비교→FN 급증 / 지역 범위 오인→FP·FN / 판별 불가 추천→FP).

### §4 정답키 생성·관리 — **단일 입력 원칙**

목록: 정답키와 제공 데이터는 **단일 입력 `<input>.csv`에서 같은 스크립트로** 나온다 → 항상 정합 / 바꾸려면 **CSV만 수정** 후 재실행(생성물 수기 편집 금지) / 생성기 책임 1줄 + `ASSIGN_DATE` 상수 고정(`datetime.now()` 미사용, 만 나이 계산에만).

### §5 실행 커맨드 — 코드블록

```bash
python eval/grade.py \
  --part1 data/<p1>_template.xlsx \
  --part2 data/<p2>_template.xlsx \
  --answer-dir eval/answer_key
# → 콘솔 표 + result.json (영역별 점수·FP/FN 목록)
```

---

## 4. eval/answer_key/INPUT_GUIDE.md — 운영자 CSV 작성법

머리말: *"이 과제의 **모든 데이터·정답은 단일 입력 `<input>.csv` 하나에서 생성**된다 … 수기로 엑셀을 고치지 말 것."* + ⚠️박스(현재 CSV는 샘플 겸 템플릿, **헤더는 절대 바꾸지 말 것**, 실제 데이터로 행 교체).

### §1 컬럼 의미/형식 — **헤더 순서 고정** 3열 표

| 컬럼 | 의미 | 형식 / 예시 |
|---|---|---|
| `policy_id` | 정책 고유키 | `YP01`… (중복 금지) |
| `field` | 분야 | `일자리`\|`주거`\|`금융복지`\|`교육문화` (이 4개만) |
| `region` | 거주 요건 | 3형식 중 하나 (§2) |
| `age_min`/`age_max` | 만 나이 하/상한 | 정수. 비우면 그 방향 제한 없음 |
| `income_max_month` | 월 소득 상한(원) | 정수. **비우면 무관** (0과 빈칸은 다름) |
| `matchable` | 명단으로 자격 확인 가능 여부 | `Y`(기본)\|`N`. **`N`=판별 불가 → 추천 0건** |

표 아래 "빈 칸 = 제약 없음. 명시 안 된 자격은 **반드시 비운다**." + **⭐박스 2개**(아래 §6 항목).

### §2 핵심축 형식 — region 3형식 등

`전국` → 전원 통과 / `<시도>` → 시도 일치 / `<시도> <자치구>` → 시도 AND 자치구. "1~2개 정책은 자치구 한정으로 두면 변별력 ↑".

### §3 각 축 설정법 — 축마다 한 줄

연령(만 나이, 생일 안 지났으면 -1) / 소득(`<=`, 중위소득 실측값 예시) / 취업(미취업·재직·무관, 단기근로 허용) / 기업규모(값 일치, 비면 무관) / 연소득(월소득과 별개 축) / 무주택(`no_house=Y`) / 학력·재학 / 혼인 — 각각 "비우면 무관" + 실제 예시 정책 ID를 괄호로.

### §4 매칭 대상 규칙 — 날짜 무관, 자격만 본다

```
대상정책 = [p for p in policies if p.matchable != 'N']   # 날짜 필터 없음
```

대상으로 만들려면 `matchable` 비우거나 `Y` + 자격 채움 / 판별 불가로 만들려면 `matchable=N` → 정답 0건 / **고정 기준일 `ASSIGN_DATE` 상수**(만 나이 계산에만, 날짜 필터엔 안 씀) + 현재 CSV 행 구성(대상 n건 + 판별 불가 m건) 나열.

### §5 멀티모달 파일 배치

포스터/이미지를 `data/<source>/`에 배치 / 파일명은 CSV의 `poster_file` 값과 **똑같이** / 코드는 포스터를 만들지 않음(운영자가 실제 파일 배치) / 비어 있어도 생성·채점은 동작.

### §6 재생성 → 채점 절차 + ⭐박스 2개

```bash
# 1) <input>.csv 를 실제 데이터로 채운다 (헤더 유지)
# 2) data/<source>/ 에 poster_file 과 같은 이름으로 파일을 넣는다
# 3) 데이터·정답 동시 생성
python3 eval/answer_key/build_dataset.py
#    → data/members.xlsx, <p1>_template.xlsx, <p2>_template.xlsx
#    → eval/answer_key/*_answer.xlsx
# 4) 학생 제출물 채점
python3 eval/grade.py --part1 … --part2 … --answer-dir eval/answer_key
```

**⭐박스 1 — `matchable=N`의 의미(판별 불가 정책)**: 자격 핵심이 회원 명단에 없는 정보(가구 소득·자녀 수 등)인 정책은 회원 DB로 가릴 수 없다 → `matchable=N`이면 자격이 맞아 보여도 **어떤 회원도 통과 못 함(추천 0건, `member_passes()` 즉시 False)**. 경계 회원도 안 심음. 그 정책의 정책표 정답에서 명단으로 못 재는 자격 칸은 **빈칸**. `matchable`은 정책표에 노출 안 하는 **운영 전용** 컬럼.

**⭐박스 2 — 매칭 축 ↔ 회원 컬럼 교집합(불변식)**: 매칭에 쓰는 모든 축은 `members.xlsx`에 대응 컬럼이 **반드시** 있어야 판단 가능. 대응 나열(연령↔`생년월일`·region↔`거주지`·… ). **새 축을 추가하려면 `<input>.csv`·`build_dataset.py`(회원 합성/경계)·`grade.py`(`P1_FIELDS`)에 짝을 함께 추가**(컬럼만 늘리면 매칭 불가). 일부 정책에서 그 축이 비어(null) 있어도 무방.

---

## 5. 비개발자 problem.md — 5규칙 (학생용 업무 브리프)

학생에게 직접 주는 유일한 파일. youth_policy `problem.md`가 모범이다. **풀이는 어렵게, 시나리오는 쉽게.**
머리말은 인용블록으로 **기준일**(나이 계산용) 한 줄: *"**오늘은 2026년 6월 28일입니다.** 회원 나이를 셀 때 이 날짜를 기준으로 하세요. (날짜는 나이 계산에만 씁니다.)"*

### 규칙 1 — 업무 브리프화

"당신은 …신입입니다" + "이번 주 당신의 일은 …" 1문단. 채점·축·F1 같은 용어 금지, 업무 이야기로만.
> 예: *"당신은 청년정책 포털 **청년몽땅정보통**에 막 들어온 신입 마케터입니다. … 이번 주 당신의 일은 — 이 정책들을 회원 한 명 한 명과 맞춰보고, '누구에게 어떤 정책을 알려줄지'를 정리하는 것입니다."*

### 규칙 2 — 정확한 판정규칙·칸 형식은 엑셀 '안내' 시트로 이전 (본문은 가리키기만)

연령·지역·소득 판정 규칙과 칸 형식은 problem.md에서 빼고, 제출 양식의 **'안내' 시트**로 옮긴다. 본문은 "안내시트대로"라고 **가리키기만**.
> 예: *"위 두 파일에는 각각 **'안내' 시트**가 따로 있습니다 … 거기에 각 칸의 이름과 적는 형식, 그리고 회원과 정책을 어떻게 맞추는지(나이·지역·소득 등 판정 규칙)가 정리돼 있어요. 먼저 '안내' 시트를 펼쳐 읽고, 적힌 대로 채우고 맞추세요."*

### 규칙 3 — 실무 메타메시지

현업의 판단(어떤 정보를 자격으로 볼지, 어떻게 맞출지는 보통 직접 정하거나 팀과 합의)을 별도 학습목표로 분리하고, "지금은 그 감각 연습"이라고 짚는다.
> 예(💡 실제 업무에서는): *"실제 업무에서는 이런 기준을 정답처럼 받아 드는 일이 드뭅니다. … 보통 본인이 직접 정하거나 팀과 합의해서 그때그때 업무에 맞게 정의합니다. 지금은 그 감각을 연습하는 단계라고 생각하면 돼요 — 주어진 규칙을 정확히 따르는 힘과 없는 정보는 무리하지 않는 판단을 함께 봅니다."*

### 규칙 4 — 친절한 파일·시트 안내

파일을 어디서 열고 어느 시트에 적는지를 손으로 짚어준다. "새 파일 만들지 말고 이 파일들을 열어 채워 저장".
> 예: *"화면 왼쪽 파일 목록에서 **`data` 폴더**를 열면 …"*, *"엑셀 파일 아래쪽 탭에서 시트 이름을 확인할 수 있어요."*, *"새 파일을 만들지 말고, 이 두 파일을 열어 그대로 채워 저장하세요."*

### 규칙 5 — 힌트만 남기기 (정답 누설 금지)

함정을 직접 설명하지 말고 **태도로만** 암시한다. "정답은 YP09" 같은 누설 절대 금지 — 일반 원칙(확실할 때만 적고 모르면 비운다)으로만.
> 예(★ 꼭 지킬 태도): *"확실히 맞을 때만 적고, 모르면 비워 둡니다. 포스터에 안 나온 조건은 빈칸으로 둡니다 … 회원 명단에 있는 정보만으로는 자격을 끝까지 확인할 수 없는 정책이 섞여 있을 수 있습니다. 그런 정책은 무리해서 추천하지 마세요. … 데이터가 없으면 무리하지 않는 게 진짜 일 잘하는 방법이에요."*

### 복붙 스켈레톤

```markdown
# <과제명> — <한 줄 업무 설명>

> **오늘은 <YYYY년 M월 D일>입니다.** 회원 나이를 셀 때 이 날짜를 기준으로 하세요. (날짜는 나이 계산에만 씁니다.)
> 시작 전에 이 글을 한 번 읽어 주세요.

## 어떤 일인가요
당신은 <도메인>에 막 들어온 신입 <직무>입니다. … 이번 주 당신의 일은 — … 입니다.
회원이 <N>명이라 손으로 하면 며칠이 갑니다. 그래서 이 일을 **AI에게 시켜서** … 만드는 게 목표예요.

## 어디에 뭐가 있나요
화면 왼쪽 파일 목록에서 **`data` 폴더**를 열면 아래가 들어 있습니다.
- **<멀티모달>** — `data` 폴더 안의 **`<source>` 폴더**에 …
- **회원 명단** — `data` 폴더의 **`members.xlsx`** (약 <N>명).
- **답을 적을 파일 2개** — 아래 ①②. (새 파일을 만들지 말고, 이 두 파일을 열어 채워 저장하세요.)

## 만들 결과물 두 가지 — 어느 파일, 어느 시트에 적는지
**① <Part1>** — … → `data`의 **`<p1>_template.xlsx`**, **'<시트>' 시트**에 적습니다.
**② <Part2>** — … → `data`의 **`<p2>_template.xlsx`**, **'<시트>' 시트**에 적습니다.
> 엑셀 파일 아래쪽 탭에서 시트 이름을 확인할 수 있어요.

## 채우는 방법은 '안내' 시트에
위 두 파일에는 각각 **'안내' 시트**가 따로 있습니다 … 먼저 '안내' 시트를 펼쳐 읽고, 적힌 대로 채우고 맞추세요.

## ★ 꼭 지킬 태도 — 확실할 때만, 모르면 비워두기
- 포스터에 안 나온 조건은 **빈칸으로** 둡니다. 없는 정보를 지어내서 채우면 틀린 답이에요.
- <명단으로 확인 못 하는 항목>은 **무리해서 추천하지 마세요.**
- 데이터가 없으면 무리하지 않는 게 진짜 일 잘하는 방법이에요.

## 💡 실제 업무에서는
<현업 판단은 직접 정의·팀 합의 — 지금은 그 감각 연습>

## 제출
답을 다 채웠으면 **위 두 파일을 저장**하면 끝입니다. 줄 순서는 점수에 영향이 없어요. <생성형은 만들지 않아도 됨>.
```

---

## 6. 작성 체크 (4문서 + problem.md 공통)

- [ ] **트리·경로**: `grade.py`=`eval/grade.py`, 정답키만 `answer_key/`. 단일 입력 CSV 1개.
- [ ] **축↔컬럼 교집합**: README §3·intention 박스·INPUT_GUIDE ⭐박스2에 **동일한 축↔컬럼 대응**이 일관 등장.
- [ ] **판별 불가(`matchable=N`)**: 4문서 전부에서 "추천 0건 / 자격 칸 빈칸 / 운영 전용" 일관 선언.
- [ ] **결정성**: README §5·intention 봉쇄 선언·evaluation §4가 "유일 정답 + 단일 입력 + seed/ASSIGN_DATE 고정"으로 수렴.
- [ ] **분리형(decouple)**: README §3가 "Part2엔 정답표 별도 제공" 명시.
- [ ] **생성형 제외**: README §4·problem.md 제출 절에 "카피/문서는 만들지 않는다" 1줄.
- [ ] **problem.md 누설 0**: 특정 정답 ID·함정 직접 설명 없음. 태도(규칙 5)로만 암시.
- [ ] **problem.md에 채점 용어 없음**: F1·Exact·축·`matchable` 같은 용어는 학생 문서에 쓰지 않는다(안내 시트·eval로만).
